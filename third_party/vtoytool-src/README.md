This directory was copied from the local `Ventoy-master/VtoyTool/` tree.

It contains the source for the multi-call `vtoytool` helper used by `vtoyboot`.
In this normalized project, `scripts/build-vtoytool.sh` builds `tools/vtoytool_*`
from these sources and recreates the Linux `vtoydump*` symlinks.
